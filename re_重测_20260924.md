# re 重测 · 2026-09-24

> **规则**：不查资料、不运行代码、不看 `正则速查表.md`、**不翻聊天记录**。
> 答不出来就写「不会」—— **这也是有效信息**，别硬编。
>
> **上次成绩（9/17）**：A1 ❌ ／ A2 ❌（答了 `abc`）／ A3 不清楚 ／ A4 不会  → **re = 1/5**
>
> 目标：看看这 8 天涨了多少。

---

## A1 · 写正则

从这行里提取**日期和时间**（只要这两部分），写出你的正则：

```
2026-09-16 10:23:45 ERROR 数据库连接失败
```

**我的答案**：

re.search(r"\4d-\2d-\2d\s\2d:\2d:\2d\s\w+\s\w+")


---

## A2 · 预测输出

```python
re.findall(r"\d+", "a1b22c333")
```

输出是什么？（说清是什么**类型**、里面是**什么**）

**我的答案**：123 整数 我对findall的印象不太深刻了，我猜应该是找所以的然后去除重复的




---

## A3 · 说区别

`re.match` / `re.search` / `re.fullmatch` 三者有什么区别？

（提示：三个都回答"从哪开始匹配"、"要不要匹配到结尾"）

**我的答案**：re.match从开头开始匹配，开头不对直接错误；search全部都匹配；fullmatch好像是任意位置开始匹配，但是不匹配到结尾。所有要不要匹配到结尾我都记不太清了 




---

## A4 · 命名分组

把 A1 的正则改成**命名分组**，并写一行代码取出日期。

**我的答案**：忘记了




---

## 自评对照表（填完再看）

| 题 | 9/17 | 今天 | 说明 |
|---|---|---|---|
| A1 | ❌ | | |
| A2 | ❌（答了 `abc`） | | |
| A3 | 不清楚 | | |
| A4 | 不会 | | |

**填完把这一页发我，我给分数。**

---

## 附：这 8 天你其实用过 re 做了不少事

（不用看，只是提醒你：分数会涨是有原因的）

- 写出了完整的日志行解析正则（`^(\d{4}-\d{2}-\d{2})\s...$`）
- 用 `re.match` 区分了正常行和脏行
- 搞清了 `^` `$` 是"整行匹配"还是"包含匹配"
- 知道 `\b` 必须配 `r""`
