# 题型识别速查（条件 → 解法）

> 用法：拿到新题**先看"条件"**，从表里找对应解法。每刷完一类新题就往里加一条。
> 建立时间：2026-09 | 维护人：自己

## 一、条件 → 解法映射（核心表）

| 看到什么条件 | 想什么解法 | 代表题 |
|---|---|---|
| **有序数组** + 找两数/目标和 | 双指针 · **夹逼** | 167、977 |
| **有序数组** + 去重/删除 | 双指针 · **占位** | 26 |
| **无序数组** + 找两数/判存在 | **哈希表** | 1 |
| 数组**原地**删除/移动元素 | 双指针 · **占位** | 283、27 |
| 链表 + 判环/找中点/倒数第 N | 双指针 · **快慢** | 141、876、19 |
| 求**连续**子数组/子串的最优解 | **滑动窗口**（待学） | 209、3 |
| 有序 + 查找某个值 | 二分查找（待学） | 704 |
| 求所有组合/排列/子集 | 回溯（待学） | 46、78 |
| 最优化 + 重叠子问题 | 动态规划（待学） | 70、198 |

## 二、双指针三形态（已掌握）

### 1. 快慢指针（链表）
- **141 环形链表**：快走 2 步、慢走 1 步；相遇 = 有环（快指针会"套圈"追上慢指针）
- **876 链表中点**：快指针到终点时慢指针正好在中点（偶数长度返回第二个中点）
- **19 删除倒数第 N**：快先走 N 步，再同步走；用 `dummy` 统一处理"删头"
- ⚠️ 关键：循环用 `while fast and fast.next`（快指针停在最后一个节点，不是 None）

### 2. 占位双指针（数组原地整理）
- **283 移动零**：slow 标记"下一个非零该放的位置"，遇到非零就交换
- **27 移除元素**：slow 标记写入位置，直接**覆盖**（尾部内容无所谓）
- **26 删除有序数组重复项**：`nums[fast] != nums[slow]` 时才写入
- ⚠️ 关键：**283 要保留全部元素 → 交换；27/26 允许丢弃尾部 → 覆盖**

### 3. 夹逼双指针（有序数组两端往中间收）
- **167 两数之和 II**：和太小 → 左指针右移；和太大 → 右指针左移；**返回下标从 1 开始**
- **977 有序数组的平方**：平方最大值必在两端 → 从后往前填结果数组
- ⚠️ 关键：**有序 → 每次能排除一整片**（O(n) 而非 O(n²)）

## 二·B、二分查找三形态（已掌握，9/16–9/19）

### 0. 二分答案的固定套路（最重要）

| 步骤 | 做什么 | 875 爱吃香蕉的珂珂 |
|---|---|---|
| **① 定范围** | 答案的 `[下界, 上界]` | `[1, max(piles)]`（0 无意义；超过最大堆没收益） |
| **② 写判定函数** | `feasible(x)` = "猜 x 可行吗"，返回 True/False | `sum(ceil(pile/x)) <= h` |
| **③ 定方向** | 真 → 答案在左 `right = mid-1`；假 → 往右 `left = mid+1`；最后 `return left` | 真 = 够快 → 找更小的 |

### ⚠️ 必须分清的两个概念（我在这里混过）

| 概念 | 是什么 | 875 里的样子 |
|---|---|---|
| **循环条件** | 控制二分何时停 —— **模板固定，永不变** | `while left <= right` |
| **判定函数** | 检验"猜的这个值可行吗" —— **每题唯一变化的部分** | `sum((p+mid-1)//mid) <= h` |

> **二分答案题的难度 100% 在判定函数上。**
> 循环条件、`mid` 的算法、`return left` 都是背下来的模板；
> 真正要动脑的只有一件事：**怎么把题目的规则翻译成一个返回 True/False 的检验函数。**

### 三个子题型

| 子题型 | 答案在哪 | 判定的是什么 | 例题 |
|---|---|---|---|
| **基础** | 数组下标 | `nums[mid] == target` | 704 |
| **找边界** | 数组下标 | `nums[mid] < t`（关键：**相等时往哪压**） | 35、34、278 |
| **答案空间** | **不在数组里** | **"猜的这个数可行吗"** | 69、875、367 |

**判断方法**：问自己"我二分的对象是**数组下标**，还是**我猜出来的一个数**？"

### 四个铁律（全是踩过的坑）

- ⚠️ **`mid = left + (right - left) // 2`** —— 少写 `//2` 会让 `mid` 恒等于 `right`，O(log n) 退化成 O(n)
- ⚠️ **"找到就 return" 只能用在答案唯一的题上**（704、35、367）；有重复的（34、278）必须压到边界
- ⚠️ **越界哨兵 = `right` 的初值 + 1**：34 题 `right = len-1` → 判 `first == len`；278 题 `right = n` → 判 `left > n`
- ⚠️ **`left` 的下界不能取 0**（875：`mid` 会取到 0 → `ZeroDivisionError`）
- ⚠️ **要不要写边界检查，看题目保证了什么**：278 保证有坏版本→不写；875 保证至少一堆→不写；34 的 target 可能超范围→**必须写**


## 三、踩过的坑（血泪清单）

- 数组/链表遍历统一写 `range(len(nums))`，**别加减 1**
- `len(nums) - 1` 只用在"当下标"时（right 指针、range 的 stop）；**"数量/长度"不减**
- **167 返回下标从 1 开始（要 +1）**；**1 题从 0 开始（不加）** —— 别搞混
- 哈希表方向：**存"自己"**（`d[num] = i`），**查"补数"**（`target - num in d`）；**先查后存**
- 循环的 `if/elif/else` 必须**覆盖所有情况**，漏 case = 死循环（977 的相等分支教训）
- **先利用题目给的条件**（有序？重复相邻？）——别一上来就上哈希表（26 用哈希 = 浪费了"有序"这个礼物）
- 提交前**走查三个边界**：相等的情况 / 单个元素 / 空数组
- **别边遍历边改列表**（除非只改"已读过的位置"）；删除用倒序遍历或双指针

## 四、已刷题记录

| 题号 | 题目 | 套路 | 关键点 |
|---|---|---|---|
| 141 | 环形链表 | 快慢指针 | 相遇判环 |
| 876 | 链表中点 | 快慢指针 | 快到头慢在中 |
| 19 | 删除倒数第 N | 快慢 + dummy | 快先走 N 步 |
| 283 | 移动零 | 占位（交换） | slow 标记写入位 |
| 27 | 移除元素 | 占位（覆盖） | 覆盖比交换快 |
| 167 | 两数之和 II | 夹逼 | 有序 + 返回 1 起 |
| 977 | 有序数组的平方 | 夹逼（从后往前填） | 最大值在两端 |
| 1 | 两数之和 | 哈希 | 存自己查补数 |
| 26 | 删除有序数组重复项 | 占位（相邻比较） | 用上"有序"条件 |
| **704** | 二分查找 | 二分·基础 | `left<=right` + `mid±1`；找到就 return |
| **35** | 搜索插入位置 | 二分·找插入点 | **找不到时 `return left`（不是 -1）** |
| **34** | 查找第一个和最后一个位置 | 二分·找边界 | `lower_bound(t)` 与 `lower_bound(t+1)-1`；**必须 guard 越界** |
| **278** | 第一个错误的版本 | 二分·找边界 | 相等不停、往左压；题目保证有坏版本 → 不用 guard |
| **69** | x 的平方根 | 二分·答案空间 | 用 `<=` 让边界统一 → 无条件 `left-1` |
| **367** | 有效的完全平方数 | 二分·答案唯一 | 找到就 return（答案唯一，不需要压边界） |
| **875** | 爱吃香蕉的珂珂 | 二分·答案空间 | 判定函数 `sum(ceil(pile/k)) <= h`；`left` 不能从 0 开始 |
| — | 206 反转链表 / 200 岛屿数量 | 指针操作 / DFS | 8 月复盘重做 |

## 五、下一步要学的题型（按优先级）

1. **链表**（206 反转 → 141 环 → 21 合并两个有序链表）← W3 主线
2. **滑动窗口**（209 长度最小的子数组 → 3 无重复字符的最长子串）← 已做过，需回炉
3. **回溯**（46 全排列 → 78 子集）
4. **动态规划**（70 爬楼梯 → 198 打家劫舍 → 53 最大子数组和）
5. **栈/单调栈**（20 有效括号 → 739 每日温度）
6. **二分进阶**（33 搜索旋转排序数组 → 1011 在 D 天内送达包裹 → 410 分割数组的最大值）
