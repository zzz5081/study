# -*- coding: utf-8 -*-
"""
════════════════════════════════════════════════════════════════════
logging 默写练习  ·  10 分钟  ·  不许查资料
════════════════════════════════════════════════════════════════════

【要写出什么】

  配置一个 logger，要求：
    · 屏幕只显示 INFO 以上
    · 文件 logging_practice.log 记录 DEBUG 以上
    · 格式长这样：  2026-09-24 15:30:00 [INFO] __main__: 消息

【默写口诀（只有结构，代码自己写）】

  ① 拿 logger
  ② 设总闸（logger 的级别）
  ③ 造 handler 1（屏幕）→ 设分闸
  ④ 造 handler 2（文件）→ 设分闸
  ⑤ 造 formatter → 设给两个 handler
  ⑥ ★ 把两个 handler 接上去 ★

  一共 6 步，大约 11 行代码。

【规则】

  · 写不出来就空着/写 TODO —— 「不会」也是有效信息，别查资料
  · 写完再往下跑验收代码（验收部分不要改）
  · 跑完把结果发我
════════════════════════════════════════════════════════════════════
"""
import logging

# ═══════════════════════════════════════════════════════════
# ↓↓↓ 在这里写配置（写 6 步）↓↓↓
# ═══════════════════════════════════════════════════════════



# ═══════════════════════════════════════════════════════════
# ↑↑↑ 写到这里（上面必须定义出一个叫 logger 的变量）↑↑↑
# ═══════════════════════════════════════════════════════════


# ─────────── 以下为验收代码，不要修改 ───────────
if __name__ == "__main__":
    logger.info("① 这条应该同时出现在【屏幕】和【文件】")
    logger.debug("② 这条只应该出现在【文件】，不出现在屏幕")

    print()
    print("=" * 62)
    print("【验收标准】")
    print("  · 屏幕上方只出现 1 行（带 [INFO] 的那条 debug 不出现）")
    print("  · logging_practice.log 里有 2 行（[INFO] 和 [DEBUG] 都有）")
    print("  · 两行都带时间戳和 [级别]")
    print("  · 自检：print(logger.handlers) 应该看到 2 个 handler")
    print("=" * 62)
