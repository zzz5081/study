# 待办清单（每次打开先看这里）

> **说明**：DeepSeek 不能主动给你发消息。所以待办写在这里 ——
> ① 你打开 PyCharm 就能看到这个文件
> ② 你说"给我今天的任务"时，我也会读它，第一句就提这些
> ③ 重要的事（考试/截止）**自己再设个手机闹钟**，那才是真正可靠的备份

---

## 9/19（周六）← 今天

### A. 收尾（15 分钟）
- [ ] `常见错误本.md` 还有未提交的改动（二分 `<` vs `<=` 那条规律）：
      `git add -A` → `git commit -m "错题本: 二分 < 与 <= 的边界语义（69题实测）"` → `git push`
- [ ] 📱 **手机闹钟：9/20（周日）18:45** ← 考试时间是 19:00（不是早上！）
- [ ] 微信问老师：**蓝桥杯校内选拔赛时间**（欠 5 天了）
- [ ] 🔴 **今晚必做：登录一次信息素养大赛平台**，确认账号密码能用
      （今天 9/19 窗口未开，最早是 9/20 06:00；但登录验证今天可以做）

### B. 复盘（20 分钟）—— 把被动看变成主动做
- [ ] 扫一遍 `常见错误本.md`（现在 81 行），**挑最容易犯的 3 条抄在纸上贴电脑旁**
      建议候选：① `/d` vs `\d`、`reture` ② `self.name = "name"` ③ 字符串比较大小写
- [ ] 新建 `leetcode/` 目录，把这几天的题解归档（每题一个文件）：
      `lc_0035_搜索插入位置.py`、`lc_0034_查找第一个和最后一个位置.py`、
      `lc_0278_第一个错误的版本.py`、`lc_0069_x的平方根.py`
      **每题文件头写 3 行注释：① 题型 ② 关键点 ③ 我踩的坑**
      → 这一步同时解决三件事：复盘（治"记不牢"）+ 归档 + 11 月简历素材

### C. 算法（60-70 分钟）—— 二分答案模板迁移
- [ ] **367. 有效的完全平方数**（10 分钟，热身）
      就是 69 题的判定条件换个问法
- [ ] **875. 爱吃香蕉的珂珂**（45 分钟，今天的重点）
      ```
      有 n 堆香蕉，第 i 堆有 piles[i] 根。警卫 h 小时后回来。
      每小时选一堆，吃 k 根（这堆不足 k 根就吃完，不能留到下一小时吃别的堆）。
      求能在 h 小时内吃完的最小速度 k。
      ```
      **和 69 题一模一样的模板**，只有两处不同：
      1. 答案范围不是 `[0, x]`，是 `[?, ?]`（想：最小速度能是多少？最大要多少？）
      2. 判定函数不是 `mid*mid <= x`，而是"**以速度 mid 吃完需要几小时？够不够 h**"
         提示：每堆需要 `ceil(pile / mid)` 小时；整数向上取整可以不用 `math.ceil`

### D. 可选（有余力才做，不勉强）
- [ ] `LogAnalyzer`：`top_errors` 拆出"错误类型"（`msg.split(":")[0].strip()`）
- [ ] W3 周一内容预习：类的**继承**（`class Dog(Animal)` / `super()`）

---

## 9/20（周日）—— W2 最后一天【有调休补课】

### 🔴 信息素养校赛 ✅ 已完成
- [x] 已考完：**57 分，排名 10000+**
- [x] 结论：**不接省赛**（省赛 10/17–11/2 撞 FastAPI 主线）→ 这件事到此结束
- [ ] 📌 复盘一句话记录：分数不重要的原因 + 下次（如果有）会怎么改进

### W2 收尾
- [ ] W2 周复盘（30 分钟，累了就顺延到周一）

---

## 9/21（周一）—— W3 Day 1

### 🎯 主线：类② 继承 + 魔术方法 + @property（约 2 小时）
- [ ] `class Dog(Animal):` + `super().__init__()`
- [ ] 方法重写（override）
- [ ] `__str__` / `__len__` / `__eq__` 等魔术方法
- [ ] `@property` 装饰器（把方法变成属性用）
- [ ] 练手：给 `LogAnalyzer` 加一个子类（比如 `JsonLogAnalyzer`，`summary()` 返回 JSON 格式）

**验收标准**：不看资料写出"带 `__init__` + 3 个方法 + 1 个继承"的类

### 📝 遗留小任务（10 分钟）
- [ ] `69` / `367` 补 `assert` 块（照 34 题的写法：`s = Solution()` + `s.方法(参数)`）
- [ ] `README.md` 索引表优化 3 行 + 补 `704` 那行
      · 278 的"判断条件变化" → 改成"相等时不停、往左压；不用 guard"
      · 69 / 875 的一行关键点补上"用 <= 统一边界" / "判定函数 sum(ceil(pile/k)) <= h"

### 🧮 每日一题（30 分钟）：206. 反转链表
- [ ] 7 月做过，现在是**回炉**——目标是不看题解独立写出来
- [ ] 做完归档到 `leetcode/lc_0206_反转链表.py`（3 行注释 + assert 块）

**W3 算法主线 = 链表**：206 反转 → 141/142 环形 → 21 合并 → 876 中点 → 19 倒数第N
（二分已经拿下，本周换赛道把链表这条线也走通）

---

## W3（9/21–9/27）补课周总览 —— 详见 Excel《综合学习路线_周执行表》W3 行
```
周一 类② 继承/魔术方法/@property + 206 反转链表   ← 类① 已提前完成
周二 argparse（半天）+ 141 环形链表
周三 logging（级别/Handler/Formatter）+ 21 合并链表
周四 re 重修（从 \d、findall 返回值、match/search 区别开始）+ 876 链表中点
周五 pathlib + Counter/defaultdict（各半天）+ 19 删除倒数第 N
周六 整合：6 项串成完整 CLI 工具
周日 验收自测：不看资料独立重写 + W3 复盘
```
验收目标：**6 项技能全部到 4/5 分**（关掉编辑器拿张纸能写出来）
