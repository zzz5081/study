# 常见错误本

> 用法：每发现一个"我会写但写错"的细节，就加一行。**只记自己真犯过的错**，不抄别人的。
> 每次写代码前扫一眼；每周日复习一遍。

## 一、Python 语法细节（手感类错误）

| 日期 | 我写错的 | 正确的 | 类型 |
|---|---|---|---|
| 9/17 | `/d` | `\d` | 反斜杠写成正斜杠 |
| 9/17 | `reture` | `return` | 拼写 |
| 9/17 | `self.name = "name"` | `self.name = name` | 把参数名当成字符串字面量 |
| 9/17 | `f"{name}: {something}"` | `f"{self.name}: {something}"` | 方法里忘写 self. |
| 9/17 | `\d(0)`、`\d(1)` | `\d{4}`、`\d{2}` | 括号是"捕获组"，量词用花括号 |
| 9/17 | `partten` | `pattern` | 拼写（能跑但错） |
| 9/18 | `if level == "error"` | `if level == "ERROR"` | **字符串比较大小写不符** → 条件永远不成立，静默收集不到数据 |
| 9/18 | `def top_errors(self, n=3): ... most_common(3)` | `... most_common(n)` | **定义了参数却没在函数体里用** → 参数变成摆设 |
| 9/22 | `choices=["INFO","EORRE","WARNING"]` | `choices=["INFO","ERROR","WARNING"]` | **字符串常量手打错**（字母顺序）→ 挡住正确的 ERROR，放行错误的 EORRE，比不写还糟 |
| 9/22 | `s.add(a)` 存【节点】，却写 `a.val not in s` 查【数值】 | 两边存同一个东西：`s.add(a)` + `a not in s`（或都用 `.val`） | **存进去和查出来不是同一种东西** → 永远不相等 → **死循环（TLE）** |
| 9/22 | `return null` | `return None` | **`null` 是 JavaScript 的写法**，Python 里没有 → `NameError` |
| 9/22 | 用 `val` 判断"是不是同一个节点" | 用节点对象本身 / `is`（身份比较） | **值可以重复** → 会返回错误的节点（反例：`[1,2,1]` 环入口是索引0，按 val 会返回索引2） |
| 9/23 | `logger.info(f"...跳过{m}行")`（`m` 是循环里的 `re.match`） | `...跳过{len(self.skipped)}行` | **循环外用循环变量** → `m` 只剩最后一轮的 Match 对象，打印出 `<re.Match object ...>` |

> ### 🔑 反复踩的坑：**在循环外面用「只在循环里赋值」的变量**
> | 日期 | 代码 | 后果 |
> |---|---|---|
> | 9/17（278题）| `while ...: mid = ...` → 循环外用 `mid` | 空数组时行为错误 |
> | 9/23 | `for ...: m = re.match(...)` → 循环外用 `m` | 打印出 Match 对象 |
>
> **循环结束后，循环变量只保留"最后一轮"的值**，它**不是**计数、不是汇总。
> 想统计数量就用 `len(容器)`；想汇总就用累加变量。
>
> **自检问句：「我这个变量是在哪里赋值的？在循环里还是循环外？」**

> ### 🔑 一条通用原则：**「存进去的东西」和「查的东西」必须一致**
> ```python
> s.add(a)              # 存的是【节点对象】
> if a.val not in s:    # 查的是【整数】     ❌ 永远不匹配
> if a not in s:        # 查的也是【节点】   ✅ 匹配
> ```
> 这类错误**不报错、只是逻辑失效**（这里表现为死循环）。
> **自检问句：「我往容器里放的是什么？我拿出来比的是什么？是同一种东西吗？」**
>
> ### 🔑 另一条：判断"同一个对象" vs "值相等"
> | 想判断 | 用什么 | 例子 |
> |---|---|---|
> | **是不是同一个对象** | `is` / 放进 `set`（默认按身份） | 环形链表的"同一个节点" |
> | **值相不相等** | `==` | 字符串、数字比较 |
>
> 自定义类若没写 `__eq__`/`__hash__`，**默认就是按内存地址**比 —— 正好适合"身份判断"。
> 一旦你写了 `__eq__`（比如"name 相同就算相等"），放进 set 的行为就变了。


> ### ⚠️ 我的高频错型：「字符串常量手打」
> | 日期 | 写错的 | 正确的 |
> |---|---|---|
> | 9/17 | `partten` | `pattern` |
> | 9/17 | `/d` | `\d` |
> | 9/18 | `"error"` | `"ERROR"` |
> | 9/22 | `"EORRE"` | `"ERROR"` |
> | 9/23 | `%(asctimer)s` | `%(asctime)s` |
>
> **共同点：不报语法错、静默失效、肉眼很难发现。**
> **对策：这类列表/常量一律【复制粘贴】，不要手打。**
> 尤其是错误信息里的提示（`invalid choice: ... choose from ...`）—— 直接照抄它给的值。

> **两个通用自检动作**（每次写完函数都做）：
> 1. **字符串比较**：`==` 两边的值，用 `print(repr(实际值))` 打出来看清楚，别靠眼睛认
> 2. **参数清点**：我定义的每个参数，在函数体里出现了吗？没出现就是摆设
>
> **调试真言：不要只看最终结果，要打印中间状态。**
> 例：`top_errors()` 返回 `[]`，可能是"没存进去"，也可能是"取的时候错了"。
> 先 `print(a.errors)` 一分开，立刻定位。

## 二、函数/方法的返回值（每次都要问自己"它返回什么"）

> ⚠️ **最常犯的一类**：**调用了、也算出来了，但结果没用上**（写了两遍）
> | 我写过的 | 问题 | 正确 |
> |---|---|---|
> | `Path("test.log")` | 只造了路径对象，没读文件 | `Path("test.log").read_text(encoding="utf-8")` |
> | `json.dumps(data, ...)` | 只算出字符串，没交出去 | `print(json.dumps(...))` 或 `return json.dumps(...)` |
> | `logging.info("x")` | 调用了，但默认级别 WARNING 把它过滤了 | 先 `basicConfig(level=logging.INFO)` |
>
> **自检问句：「这个函数有返回值吗？我接住了吗？」**

| 接口 | 返回什么 | 记错的样子 |
|---|---|---|
| `re.findall(r"\d+", "a1b22c333")` | `['1', '22', '333']`（**字符串列表**） | 以为是 `'abc'` |
| `re.search(...)` | `Match` 对象或 `None`（要用 `.group()` 取值） | 以为直接返回字符串 |
| `json.dumps(obj)` | **一个字符串**（不会自己打印） | 以为调用就会输出 |
| `Counter.most_common(2)` | `[('b', 3), ('a', 2)]`（**元组列表**，按次数降序） | 以为是 dict |
| `dict.get(k, 默认值)` | 有这个 key 就返回它的值，没有就返回默认值 | 不知道是什么 |
| `defaultdict(int)` 取不存在的 key | **自动创建**并给默认值 `0`，不会 KeyError | 以为取不到 |
| `Path("a.txt")` | **只是一个路径对象**，不会读文件 | 以为等于读文件 |

## 三、概念区分（容易混）

| 概念 A | 概念 B | 区别 |
|---|---|---|
| 类属性 `count = 0`（写在 class 里） | 实例属性 `self.count = 0`（写在 `__init__` 里） | 类属性**所有实例共享**（`A.count`）；实例属性**每个对象各一份** |
| `re.match` | `re.search` | match 只从**开头**匹配；search 全文找 |
| `re.fullmatch` | 上面两个 | 必须**整串完全匹配** |
| `logging.info()` | `logging.getLogger(__name__).info()` | 后者带**名字**（能看出是哪个模块打的），可单独配级别 |
| 类属性 `count = 0` | 全局变量 | **类属性不是全局变量**，它是"属于这个类"的属性 |

## 四、二分查找（9/16–9/17）

| 场景 | 坑 | 正确做法 |
|---|---|---|
| lower_bound 返回值 | 范围是 `[0, len(nums)]`，`len` 也合法 | 调用前必须 `if first == len(nums) or nums[first] != target` |
| `or` 顺序 | `nums[first] != target or first == len(nums)` | **越界检查必须放前面**（短路求值） |
| 找到 target 时 | 立即 `return mid` → 有重复元素时拿不到 first/last | 相等时**不停**，决定往哪边压 |
| 函数末尾 | 忘了 `return` → 返回 `None` | 每个分支都要有出口 |
| 9/17 278题 | `mid = left + (right - left)` **少写 `// 2`** | `mid = left + (right - left) // 2` |
| 9/17 278改造版 | `return -1 if left == n else left`（边界值错 + 漏 else） | `return -1 if left > n else left` |

> **越界哨兵值的通用公式：`哨兵 = right 的初值 + 1`**（= 最后一个合法下标 + 1）
> - 34 题：`right = len(nums) - 1` → 哨兵 = `len(nums)` → 判 `first == len(nums)`
> - 278 题：`right = n` → 哨兵 = `n + 1` → 判 `left > n`
>
> 三代（三元）表达式语法：`值1 if 条件 else 值2` —— **`if` 和 `else` 必须成对，漏 `else` 是语法错误**。
> 记不住就用两行版：`if 越界: return 哨兵` 然后 `return left`。

> **`// 2` 不能省**：`left + (right - left)` 代数上**恒等于 `right`**（left 抵消了），
> 所以 mid 永远取最右端 → 每次只缩 1 格 → 复杂度从 O(log n) 退化成 O(n)。
> 实测（n=10万，第一个版本就坏）：你的版本调用 100000 次，正确版本 **16 次**。
> `left + (right - left) // 2` 这个写法有两个作用：① 防整数溢出（C++/Java 需要）② **取中点**。
> 只保留①丢掉② = 白写。

> **`<` 和 `<=` 决定的是"left 停在哪"，进而决定返回值要不要分类讨论**（9/18，69 题实测）
>
> | 判定写法 | while 结束后 `left` 停在 | 返回值 |
> |---|---|---|
> | `if mid*mid <  x: left = mid+1` | **第一个 `mid² >= x`** 的位置 | 要分类：`left if left*left<=x else left-1` |
> | `if mid*mid <= x: left = mid+1` | **第一个 `mid² >  x`** 的位置 | **统一 `left - 1`** |
>
> 两种都对（都穷举验证过 0 错误），但**第二种少一次分类讨论 = 少一个出错点**。
> **规律：想让返回值统一，就让边界落在"第一个不满足条件"的位置。**
> 做题时先问：我要的是"第一个满足"还是"最后一个满足"？再决定 `<` 还是 `<=`。
