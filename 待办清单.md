# 待办清单（每次打开先看这里）

> **说明**：DeepSeek 不能主动给你发消息。所以待办写在这里 ——
> ① 你打开 PyCharm 就能看到这个文件
> ② 你说"给我今天的任务"时，我也会读它，第一句就提这些
> ③ 重要的事（考试/截止）**自己再设个手机闹钟**，那才是真正可靠的备份

---

## 9/19（周六）← 今天

### A. 收尾（15 分钟）
- [ ] `常见错误本.md` 还有未提交的改动（二分 `<` vs `<=` 那条规律）：
      `git add -A` → `git commit -m "错题本: 二分 < 与 <= 的边界语义（69题实测）"` → `git push`
- [ ] 📱 **手机闹钟：9/20（周日）18:45** ← 考试时间是 19:00（不是早上！）
- [ ] 微信问老师：**蓝桥杯校内选拔赛时间**（欠 5 天了）
- [ ] 🔴 **今晚必做：登录一次信息素养大赛平台**，确认账号密码能用
      （今天 9/19 窗口未开，最早是 9/20 06:00；但登录验证今天可以做）

### B. 复盘（20 分钟）—— 把被动看变成主动做
- [ ] 扫一遍 `常见错误本.md`（现在 81 行），**挑最容易犯的 3 条抄在纸上贴电脑旁**
      建议候选：① `/d` vs `\d`、`reture` ② `self.name = "name"` ③ 字符串比较大小写
- [ ] 新建 `leetcode/` 目录，把这几天的题解归档（每题一个文件）：
      `lc_0035_搜索插入位置.py`、`lc_0034_查找第一个和最后一个位置.py`、
      `lc_0278_第一个错误的版本.py`、`lc_0069_x的平方根.py`
      **每题文件头写 3 行注释：① 题型 ② 关键点 ③ 我踩的坑**
      → 这一步同时解决三件事：复盘（治"记不牢"）+ 归档 + 11 月简历素材

### C. 算法（60-70 分钟）—— 二分答案模板迁移
- [ ] **367. 有效的完全平方数**（10 分钟，热身）
      就是 69 题的判定条件换个问法
- [ ] **875. 爱吃香蕉的珂珂**（45 分钟，今天的重点）
      ```
      有 n 堆香蕉，第 i 堆有 piles[i] 根。警卫 h 小时后回来。
      每小时选一堆，吃 k 根（这堆不足 k 根就吃完，不能留到下一小时吃别的堆）。
      求能在 h 小时内吃完的最小速度 k。
      ```
      **和 69 题一模一样的模板**，只有两处不同：
      1. 答案范围不是 `[0, x]`，是 `[?, ?]`（想：最小速度能是多少？最大要多少？）
      2. 判定函数不是 `mid*mid <= x`，而是"**以速度 mid 吃完需要几小时？够不够 h**"
         提示：每堆需要 `ceil(pile / mid)` 小时；整数向上取整可以不用 `math.ceil`

### D. 可选（有余力才做，不勉强）
- [ ] `LogAnalyzer`：`top_errors` 拆出"错误类型"（`msg.split(":")[0].strip()`）
- [ ] W3 周一内容预习：类的**继承**（`class Dog(Animal)` / `super()`）

---

## 9/20（周日）—— W2 最后一天【有调休补课】

### 🔴 信息素养校赛 ✅ 已完成
- [x] 已考完：**57 分，排名 10000+**
- [x] 结论：**不接省赛**（省赛 10/17–11/2 撞 FastAPI 主线）→ 这件事到此结束
- [ ] 📌 复盘一句话记录：分数不重要的原因 + 下次（如果有）会怎么改进

### W2 收尾
- [ ] W2 周复盘（30 分钟，累了就顺延到周一）

---

## 9/21（周一）—— W3 Day 1 ✅ 全部完成

### 🎯 主线：类② 继承 + 魔术方法 + @property ✅
- [x] `class JsonLogAnalyzer(LogAnalyzer)` + 只重写 `report()`
- [x] `__str__`（print 出 `LogAnalyzer(test.log,有效6行)`）
- [x] `@property error_count`
- [x] 踩坑并修掉：缺 `import json` + `json.dumps()` 返回值没人用（错误会排队）

**验收**：不看资料写出"带 `__init__` + 3 个方法 + 1 个继承"的类 → 提前达成（W3 最难的一块，第一天拿下）

### 📝 遗留小任务
- [x] `69` / `367` 补 `assert` 块
- [ ] `README.md` 索引表优化 3 行 + 补 `704` 那行 ← **唯一没做的，明天顺手 2 分钟**

### 🧮 每日一题：206. 反转链表 ✅
- [x] 独立写对（踩坑：`while cur.next` 漏掉最后一个节点、`return cur` 应为 `return prev`）
- [x] 学会 **dummy 哑节点 + tail** 套路
- [x] 学会「验证你的验证工具」：测试用的 `build()` 自己先坏过
- [x] 归档 `leetcode/lc_0206__反转链表.py`（注释 + assert 块）

### 今天的时间账
14:52 开始 → 16:56 提交（**2 小时**完成全部任务，含最难的继承）

---

## 9/22（周二）—— W3 Day 2

### 🎯 主线（半天）：argparse ✅ 完成
- [x] `ArgumentParser()` / `add_argument()`
- [x] **位置参数** vs **可选参数**（`--level` / `-n`）
- [x] `default=` / `type=int` / `help=` / `choices=` / `action="store_true"`
- [x] 踩坑并修掉：位置参数不能写 `required=`；`"_n"` 少了横杠变成位置参数；
      **`choices=["INFO","EORRE",...]` 拼错 → 挡住正确的 ERROR、放行错的**（比不写还糟）
- [x] 给 `LogAnalyzer` 加了 CLI 入口，三条命令都跑通
- [x] 额外搞懂：argparse 的 **OOP 本质**（对象→方法→属性）、`-h`/`--help` 是**同一参数的两个名字**、
      短名 vs 长名的真实区别是**横杠数量**（不是字符数）、`dest` 规则

### 🧮 每日一题：141. 环形链表 ✅
- [x] 独立写对（含最刁钻的**单节点自环**边界）
- [x] `while fast and fast.next` 两个条件缺一不可 —— 又一次遇见**短路求值**
- [x] 自写 `build_cycle(vals, pos)`：**先把节点放进 list**，才能让尾节点指回索引 pos
- [x] 归档 `leetcode/lc_0141_环形链表.py`（注释 + assert 块）

### 📝 142 环形链表 II —— **下次做**
- [ ] 代码只多 3 行（相遇后 p 回 head，两个指针同步走到再相遇 = 入口）
- [ ] 难点是**证明**（设 a=头到入口、b=入口到相遇点、c=环长 → 相遇时快走 a+b+kc、慢走 a+b，快是慢两倍 → a = kc-b）
- [ ] 已踩过的坑记在错题本：「存进去的」和「查的」必须是同一种东西（存节点却查 val → 死循环）

### 📝 README 索引表 ✅（已补全 704/206/141，并改写 278/69/875）

---

## 9/23（周三）—— W3 Day 3

### 🎯 主线：logging ✅ 完成（感冒，提前收工）
- [x] `getLogger(__name__)` / `setLevel` / `StreamHandler` + `FileHandler` / `Formatter`
- [x] 给 `LogAnalyzer.parse()` 接了日志（ERROR 文件不存在 / DEBUG 跳过脏行 / INFO 解析完成）
- [x] **验收通过**：屏幕 `[INFO] 解析完成: 有效6行,跳过1行`；`log_analyzer.log` 有 DEBUG 记录
- [x] 三层问题全都抓出来并修掉（每层都被上一层掩盖）：
      ① `setup_logging()` 造了 handler 但**没 `addHandler`** → 0 字节的日志文件
      ② `%(asctimer)s` 拼写错（handler 一接上就会炸）
      ③ `.get(args.level)` 少默认值 → 打印 `None`
- [x] 另抓到一个**反复踩的坑**：**循环外用循环变量**
      `logger.info(f"...跳过{m}行")` 里的 `m` 是 `re.match` 的结果 → 打印出 Match 对象
      正解：`len(self.skipped)`（278 题的 `mid` 是同一类错）
- [x] `.gitignore` 加上 `log_analyzer.log`（⚠️ 不能写 `*.log`，会误伤 test.log）

### 🧮 21. 合并两个有序链表 ⬅️ **顺延到明天**
- [ ] 复用 **dummy 哑节点 + tail** 套路（206 学过的）
- [ ] 归档 `leetcode/lc_0021_合并两个有序链表.py`（复用 206 的 `build` / `to_list`）

---

## 9/24（周四）—— W3 Day 4

### 先把昨天顺延的补上（40 分钟）
- [ ] **21. 合并两个有序链表**（30 分钟）→ 归档
- [ ] 复查 logging 的 4 条验收（10 分钟）：
      `python log_analyzer.py test.log` ／ `type log_analyzer.log` ／
      `python log_analyzer.py 不存在.log` ／ `python log_analyzer.py test.log --level ERROR -n 5`

### 🎯 主线（约 2 小时）：re 重修 —— 从最基础开始
> 9/17 实测 re 只有 1/5，而且是因为"用过但不会"。这次**不假设基础**：
- [ ] `\d` `\w` `\s` 是什么（**必须配 `r"..."`**，`\b` 忘加 r 会变成退格符静默失效）
- [ ] **`re.findall` 返回什么**（字符串列表 / 有括号时是元组列表）
- [ ] `re.match` vs `re.search` vs `re.fullmatch`（match 只锁开头，**不锁结尾**）
- [ ] `m.groups()` / `m.group(1)` / 命名分组 `(?P<name>...)`
- [ ] `^` `$` 决定整行匹配 vs 包含匹配
- [ ] 练手：把 `LogAnalyzer` 的脏行统计改成用**命名分组**重写一遍
- [ ] **验收**：不看资料写出一个带命名分组的正则，并解释贪婪 vs 非贪婪

### 🧮 每日一题（30 分钟）：876. 链表中点
- [ ] 快慢指针（快走 2、慢走 1；快到头时慢正好在中点）
- [ ] 归档 `leetcode/lc_0876_链表中点.py`

（如果感冒还没好，就只做「顺延的 40 分钟」那部分，主线往后推 —— 你还有缓冲）

---

## 9/24（周四）—— W3 Day 4 ✅ 完成

### 🎯 主线：logging ✅（1/5 → **5/5**）
- [x] 默写 11 行配置：双 handler（屏幕 INFO / 文件 DEBUG）+ Formatter + addHandler **全对**
- [x] 诊断练习 5/5（缺 `addHandler` / 为什么 0 字节 / lastResort 机制）
- [x] 踩坑并修掉：`addFilter` vs `setFormatter`、`console.addHandler(logger)` 方向反了
- [x] 搞清两条核心机制：
      · **两道闸**：logger 一条 + 每个 handler 一条，两都过才输出；**总闸是最高权限**
      · **`logging.INFO`（常量，设/比）vs `logging.info()`（函数，发日志）** —— 区别就在大小写
- [x] 学会级别决策框架：「这条日志出现时，**谁**需要做什么？」
- [x] 纠正一个大误解：logging **≠ 注释** —— 被过滤的日志，**里面的代码照样执行**
      （实测：`logging.debug(f"...{expensive()}")` 即使不打印，`expensive()` 也被调用了）
- [x] 日志带上上下文：`logger.error(f"没有找到该项目,{self.path}")`
- [x] 顺手验证「配置先于使用」：把 `addHandler` 挪到 `debug` 之前 → `diagnose.log` 有内容了

### 🎯 主线：re 重修 ✅（1/5 → **3/5**）
- [x] **重测**（9/17 原题）：1.5/5 —— 8 天几乎没涨
- [x] 诊断出原因：**在 log_analyzer 里一直用我给的正则，从没自己从零写过**
- [x] 重修后纠正了 4 个点：
      ① `\d{4}`（不是 `\4d`，符号在前量词在后）
      ② `findall` 返回**字符串列表**，**不去重**（曾经的错误想象）
      ③ `match` 锁开头 / `search` 都不锁 / `fullmatch` 锁两端
      ④ `match/search/fullmatch` 返回 **Match 对象**（不是列表！）—— 要和 `findall` 分清
- [x] 命名分组 `(?P<phone>1[3-9]\d{9})` 语法会了（还差"先 re.search 再取值"这一步的肌肉记忆）

### ⏭ 顺延到明天
- [ ] 21. 合并两个有序链表
- [ ] 876. 链表中点

---

## 9/25（周五）—— W3 Day 5

### 🎯 主线 A（半天）：pathlib
- [ ] `Path("a/b/c.txt")` 的用法；`Path("dir") / "file.txt"` 用 `/` 拼路径
- [ ] 读：`read_text()` / `read_bytes()`；写：`write_text()`
- [ ] 路径属性：`.name` / `.stem` / `.suffix` / `.parent` / `.resolve()`
- [ ] 遍历：`iterdir()` / `glob("*.txt")` / `rglob()`
- [ ] 建目录：`mkdir(exist_ok=True)`；判断：`exists()` / `is_file()`
- [ ] **验收**：不看资料写出「遍历某目录下所有 .log、按后缀分组、打印文件名+大小」

### 🎯 主线 B（半天）：Counter / defaultdict
- [ ] `Counter` 常用法：`most_common(n)` / `update()` / `subtract()` / `elements()` / `total()`
- [ ] `defaultdict(int)` / `defaultdict(list)` 的典型场景
- [ ] 两者的区别：`Counter` 是"计数专用 dict"（自动补 0 + 有统计方法）；`defaultdict` 是"能指定默认值的 dict"
- [ ] 和普通 `dict.get(k, 0)` 的取舍
- [ ] **验收**：说清两者差别 + 各写一个适用场景

### 🧮 算法（60 分钟，补顺延的）
- [ ] **21. 合并两个有序链表**（dummy + tail 套路）→ `lc_0021`
- [ ] **876. 链表中点**（快慢指针）→ `lc_0876`

### 📝 间隔重复（各 5 分钟，做任务前先做）
- [ ] **默写一遍 logging 的 6 步配置**（第 3 遍就刻进去了）
- [ ] **默写一遍 re 的 4 个点**（`\d{4}` / findall 返回值 / 三函数锁两端 / 命名分组）

---

## W3（9/21–9/27）补课周总览 —— 详见 Excel《综合学习路线_周执行表》W3 行
```
周一 类② 继承/魔术方法/@property + 206 反转链表   ✅ 类①+类② 一天做完
周二 argparse（半天）+ 141 环形链表                 ✅
周三 logging + 21 合并链表                          ✅ logging 完成 / 21 顺延
周四 re 重修 + 876 链表中点                          ✅ re 重修完成 / 876 顺延
周五 pathlib + Counter/defaultdict + 21 + 876        ← 明天
周六 整合：6 项串成完整 CLI 工具 + 各科速查表（含 logging速查.md、142 的证明）
周日 验收自测：不看资料独立重写 + W3 复盘
```
验收目标：**6 项技能全部到 4/5 分**（关掉编辑器拿张纸能写出来）

**当前进度**：类 4/5 ✅ ／ argparse 4/5 ✅ ／ logging 5/5 ✅ ／ re 3/5 🟡 ／ pathlib ⬜ ／ Counter ⬜
