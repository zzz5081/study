# 待办清单（每次打开先看这里）

> **说明**：DeepSeek 不能主动给你发消息。所以待办写在这里 ——
> ① 你明天打开 PyCharm 就能看到这个文件
> ② 你说"给我今天的任务"时，我也会读它，第一句就提这些
> ③ 重要的事（考试/截止）**自己再设个手机闹钟**，那才是真正可靠的备份

---

## 9/18（周五）

### A. 先把昨天(9/17)的尾巴收掉
- [ ] `log_analyzer.py`：`partten` → `pattern`（拼写错误，趁早改）
- [ ] `git add -A` → `commit` → `push`（面向对象入门 + 错题本 + 这个文件）
- [ ] LeetCode 提交 **34 题** 和 **278 题**，确认 AC，记下击败百分比

### B. 复盘（约 10 分钟，就是你说的"明天再复盘"）
- [ ] 把这条加进 `正则速查表.md`：
      **`^` / `$` 决定"整行匹配"还是"包含匹配"** —— 没有锚点时，
      脏行 `...（INFO/ERROR/WARNING 各来几条）` 会被误算成一条日志
- [ ] 从头扫一遍 `常见错误本.md`（9/17 新增了 5 条：`/d`、`reture`、
      `self.name="name"`、`partten`、`mid` 少写 `//2`、`left == n` 边界错）

### C. 今天的任务
- [ ] `LogAnalyzer` 三个进阶：
      `summary()` 返回 dict ／ `top_errors(n=3)` ／ `try...except FileNotFoundError`
- [ ] 微信问老师：**蓝桥杯校内选拔赛时间**（问了 3 天了，今天就发出去）

### D. 别忘
- [ ] 📱 手机设闹钟：**9/20（周日）08:45** —— 信息素养校赛 9:00 开始

---

## 周日 9/20（W2 最后一天）
- [ ] **09:00–11:00 信息素养校赛**（准备 0 小时；只在前一晚登录一次确认账号能用）
- [ ] W2 周复盘（30 分钟）
- [ ] ⚠️ 省赛 10/17–11/2 撞 FastAPI 主线 → **校赛考完就结束，不接省赛**

---

## W3（9/21–9/27）补课周 —— 详见 Excel《综合学习路线_周执行表》W3 行
```
周一 类① 基础（__init__/self/实例属性/方法）  ← LogAnalyzer 已完成，可提前进类②
周二 类②（继承/魔术方法/@property）+ argparse
周三 logging（级别/Handler/Formatter）
周四 re 重修（从 \d、findall 返回值、match/search 区别开始）
周五 pathlib + Counter/defaultdict
周六 整合：6 项串成完整 CLI 工具
周日 验收自测：不看资料独立重写
```
验收目标：**6 项技能全部到 4/5 分**（关掉编辑器拿张纸能写出来）
