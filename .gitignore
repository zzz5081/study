.idea/
__pycache__/
*.pyc
venv/

# 程序自己生成的日志文件（不是数据，不该进仓库）
# ⚠️ 只逐个列出 —— 不要写 *.log，因为 test.log 是要留在仓库里的测试数据
log_analyzer.log
logging_practice.log
diagnose.log

# 含数据库密码的本地脚本/配置，不要上传 GitHub
sqlalchemy_demo.py
.env
db_config.py
*_secret.py
