# 待办清单（每次打开先看这里）

> **说明**：DeepSeek 不能主动给你发消息。所以待办写在这里 ——
> ① 你打开 PyCharm 就能看到这个文件
> ② 你说"给我今天的任务"时，我也会读它，第一句就提这些
> ③ 重要的事（考试/截止）**自己再设个手机闹钟**，那才是真正可靠的备份

---

## 9/18（周五）← 今天

### A. 收尾巴 ✅ 全部完成
- [x] `log_analyzer.py`：`partten` → `pattern`（已改）
- [x] `git commit 2955bd0` + `push`（4 文件 / 168 行，HEAD == origin/main）
- [ ] LeetCode 提交 **34 题** 和 **278 题**，确认 AC，记下击败百分比 ← **只差这个**

### B. 复盘（10 分钟）
- [x] `正则速查表.md` 已更新（DeepSeek 代做）：
      · 新增「`^` `$` 决定整行匹配 vs 包含匹配」（含脏行实测）
      · 修正坑①（原来说"必须用 search"，其实 match 写全模式也对——和你的代码矛盾，已理顺）
      · 新增坑⑨（不加锚点会误匹配脏行）、坑⑩（括号数 = 解包数）
      · 第六节补上「4 组 vs 6 组」两个日志模式版本
- [ ] 从头扫一遍 `常见错误本.md`（不用背，扫一眼就行）

### C. 今天的【主任务】✅ 全部完成（9/18 下午）
- [x] **进阶1 `summary()`** → `return dict(self.levels)`（自己从 report() 第 30 行推出来的）
- [x] **进阶2 `top_errors(n=3)`** → 中间踩了 2 个坑并修掉：
      · `if level == "error"` 大小写不符 → 数据一条都没进来（静默失效）
      · `most_common(3)` 写死 → `n` 参数变摆设
- [x] **进阶3 `try / except FileNotFoundError`** → 一次写对（含关键的 `return`）
- [x] 验收通过：`summary()` / `top_errors()` / `top_errors(1)` / 文件不存在不崩

### C-2. 收尾（10 分钟）
- [ ] `git add -A` → `commit` → `push`（log_analyzer 三个进阶 + 错题本 + 正则速查表 + 待办清单）
      commit message 建议：`LogAnalyzer 进阶: summary/top_errors/异常处理 + 错题本/速查表更新`
- [ ] LeetCode 提交 **34 题** 和 **278 题**，确认 AC，记下击败百分比

### D. 2 分钟的事
- [ ] 📱 **手机设闹钟：9/20（周日）08:45**
- [ ] 微信问老师：**蓝桥杯校内选拔赛时间**（欠 4 天了，今天必须发出去）

---

## 周日 9/20（W2 最后一天）
- [ ] **09:00–11:00 信息素养校赛**（准备 0 小时；只在前一晚登录一次确认账号能用）
- [ ] W2 周复盘（30 分钟）
- [ ] ⚠️ 省赛 10/17–11/2 撞 FastAPI 主线 → **校赛考完就结束，不接省赛**

---

## W3（9/21–9/27）补课周 —— 详见 Excel《综合学习路线_周执行表》W3 行
```
周一 类① 基础（__init__/self/实例属性/方法）  ← LogAnalyzer 已完成，可直接进类②
周二 类②（继承/魔术方法/@property）+ argparse
周三 logging（级别/Handler/Formatter）
周四 re 重修（从 \d、findall 返回值、match/search 区别开始）
周五 pathlib + Counter/defaultdict
周六 整合：6 项串成完整 CLI 工具
周日 验收自测：不看资料独立重写
```
验收目标：**6 项技能全部到 4/5 分**（关掉编辑器拿张纸能写出来）
